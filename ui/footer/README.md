Open the mockup in a browser:

1. Full mockup (header + list + footer): open `movie-footer-mockup.html` in the `ui/footer/` folder.
2. Footer-only mockup (list + footer overlay): open `movie-footer-only-mockup.html` in the `ui/footer/` folder.

Notes:
- These are static HTML/CSS mockups for quick preview.
- If you want a Figma file or a React Native/Flutter component, tell me and I will generate it.
